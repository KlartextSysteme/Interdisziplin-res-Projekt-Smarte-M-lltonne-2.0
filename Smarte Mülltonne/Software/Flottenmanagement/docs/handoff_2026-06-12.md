# Handoff 2026-06-12 — Web-App, Render-Deploy und offene Baustellen

Dieses Dokument ist als schneller Einstieg fuer einen anderen Agenten gedacht.
Es beschreibt den aktuellen Stand der Demo nach dem Render-Deployment und die
naechsten sinnvollen Schritte. Die aelteren Dokumente `architecture.md` und
`api_contract.md` enthalten weiterhin viel Kontext, sind aber an einigen Stellen
historisch gewachsen.

## Aktueller Stand

- Repo: `CosmicPlayground16/Interdisziplin-res-Projekt-Smarte-M-lltonne-2.0`
- App-Verzeichnis: `Smarte Mülltonne/Software/smart-bin`
- Render-Blueprint: `render.yaml` im Repo-Root
- Backend-Service: `smarte-muelltonne-2-api`
- Frontend-Service: `smarte-muelltonne-2-dashboard`
- Dashboard-URL: `https://smarte-muelltonne-2-dashboard.onrender.com/dashboard/`
- API-Healthcheck: `https://smarte-muelltonne-2-api.onrender.com/health`
- Frontend wird als statischer Next-Export gebaut und mit `serve` ausgeliefert.
- Backend ist FastAPI mit SQLite-Seed fuer 35 Tonnen.
- Der Chat-Agent nutzt Groq (`GROQ_API_KEY`) und braucht keinen Anthropic-Key mehr.
- Solar ist aus der sichtbaren Web-App entfernt. In DB/Seed existieren alte
  Felder wie `solar_output_w` noch aus Kompatibilitaetsgruenden, werden aber in
  UI, WebSocket-Payload, Agent-Prompt und `/energy` nicht mehr verwendet.
- Der Truck nutzt fuer Depot-Fahrten jetzt OSRM-Geometrien statt Luftlinie und
  faehrt nach dem Entladen wieder per Strassenroute zur Sammelroute zurueck.
- Die Web-App wurde visuell in Richtung dunkler Leitstand mit gelbem
  Touchpanel-Akzent ueberarbeitet.

## Wichtige aktuelle Commits

- `898aa7e8` — Demo auf Groq umgestellt und Solar-UI entfernt.
- `9f4d4b05` — Truck-Simulator in das Backend integriert.
- Aktuelle lokale Aenderungen nach diesem Handoff: Depot-Routing-Fix,
  Truck-Kapazitaet auf `1000.0`, dunkles UI-Redesign.

## Render-Details

Der Frontend-Build hatte auf Render mehrere Probleme mit fehlenden internen
Next-Dateien. Aktueller Workaround:

- Next `15.4.10`
- `output: "export"` in `frontend/next.config.ts`
- `webpackBuildWorker: false`
- explizite Dependencies fuer `@next/react-refresh-utils` und `@babel/runtime`
- Render-Build kopiert diese beiden Pakete in Nexts `dist/compiled`-Pfad
- Render startet das Frontend mit `npx serve@14 out -l $PORT`

Das ist pragmatisch fuer die Demo, aber nicht schoen. Wenn spaeter Zeit ist,
sollte das Frontend-Deployment entweder auf einen saubereren Next/Vite-Static
Build umgestellt oder die Render/Next-Paketproblematik sauber entfernt werden.

## Backend und Simulation

Der simulierte Muellwagen laeuft aktuell im FastAPI-Lifespan als Background-Task:

- Einstieg: `backend/main.py`
- Runner: `backend/services/truck_simulator.py`
- Live-State: `backend/truck_state.py`
- WebSocket-Payload: `backend/routers/ws.py`
- Sim-Speed/Pause: `backend/routers/sim.py`

Der Runner startet am Depot und wartet auf eine aktive Route. Sobald `POST
/routes/plan` eine Route mit Wegpunkten erzeugt, bewegt der Runner den Truck
entlang der Route-Geometrie, schreibt Positionen in `truck_state` und leert
Tonnen bei Ankunft.

Lokal wurde verifiziert:

```bash
cd "Smarte Mülltonne/Software/smart-bin/backend"
PORT=8899 ./.venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8899
curl -X PUT http://127.0.0.1:8899/sim/speed \
  -H 'Content-Type: application/json' \
  -d '{"speed":20,"paused":false}'
curl -X POST http://127.0.0.1:8899/routes/plan
curl http://127.0.0.1:8899/truck/status
```

Danach aendern sich die Truck-Koordinaten und `load_percent` steigt.

## Truck-Depot-Routing

Der Truck faehrt die normale Sammelroute entlang der OSRM-Geometrie. Fuer
Depot-Umwege nutzt er jetzt ebenfalls OSRM:

- `current_pos -> depot`
- Entladepause am Depot
- `depot -> resume_pos`, falls er mitten in der Sammelroute wegen voller Ladung
  entladen musste
- Nach Routenende faehrt er nur noch zum Depot und bleibt danach idle

Die fruehere Luftlinien-Interpolation ist damit fuer Depot-Fahrten entfernt.
Zusaetzlich wurde `TRUCK_CAPACITY_UNITS` von `600.0` auf `1000.0` erhoeht, damit
die Demo nicht nach wenigen vollen Tonnen sofort wieder zum Betriebshof faehrt.

Implementiert in:

- `backend/services/truck_simulator.py`
- `_route_coords_between(...)`
- `_drive_geometry(...)`
- `_unload_at_depot(...)`
- `backend/services/routing.py` fuer OSRM-Helfer

## Chat-Agent

Der Agent laeuft ueber Groq:

- `backend/agent/planner.py`
- `backend/agent/tools.py`
- `backend/agent/prompts.py`

Wichtig: Die Tools verwenden jetzt `AGENT_BASE_URL` oder fallback
`http://127.0.0.1:$PORT`. Das ist fuer Render wichtig, weil der Agent im selben
Backend-Service laeuft und nicht hart `localhost:8000` verwenden darf.

Render braucht fuer den Chat:

```text
GROQ_API_KEY=gsk_...
```

Kein `ANTHROPIC_API_KEY` mehr notwendig.

## UI-Redesign

Die Web-App wurde von der hellen Prototype-Optik auf einen dunklen, kompakteren
Leitstand umgestellt. Die Richtung orientiert sich am Touchpanel-Stil:

- Dark Mode als Grundstimmung
- neutraler grauer Hintergrund
- Gelb als klarer Akzent
- kompakte, scanbare Panels
- Karte als zentrale Arbeitsflaeche
- linke Liste dichter und informativer
- rechte Seite als Command-/Status-Bereich
- responsive Fallback: auf kleinen Screens werden Fleet, Karte und Panel
  gestapelt; der Route-Button wird ikonisch

Konkrete Frontend-Dateien:

- `frontend/app/dashboard/page.tsx`
- `frontend/app/dashboard/components/FleetPanel.tsx`
- `frontend/app/dashboard/components/LeafletMap.tsx`
- `frontend/app/dashboard/components/ChatInterface.tsx`
- `frontend/app/dashboard/components/EnergyPanel.tsx`
- `frontend/app/dashboard/components/SecurityPanel.tsx`
- `frontend/app/dashboard/components/AlertBanner.tsx`
- `frontend/app/globals.css`

## Empfohlene naechste Reihenfolge

1. Backend live mit einer echten Route beobachten, besonders den Fall
   "Kapazitaet voll -> Depot -> zurueck zur Route".
2. Mit laufender API die neue UI visuell gegen echte 35 Tonnen pruefen.
3. Frontend lokal bauen:

```bash
cd "Smarte Mülltonne/Software/smart-bin/frontend"
npm run build
```

4. Backend lokal testen:

```bash
cd "Smarte Mülltonne/Software/smart-bin/backend"
./.venv/bin/python -m compileall main.py services routers agent
```

5. Deploy ueber Render/Blueprint auf `main`.

Aktuelle Verifikation:

```bash
python -m py_compile "Smarte Mülltonne/Software/smart-bin/backend/services/truck_simulator.py"
cd "Smarte Mülltonne/Software/smart-bin/frontend"
npm run build
```

Zusaetzlich wurden lokale Chrome-Screenshots fuer Desktop und kleine Breite
erstellt. Der In-App-Browser blockte `localhost`/`127.0.0.1`, daher lief die
visuelle Pruefung ueber Chrome Headless gegen den statischen Export.

## Hinweise fuer den naechsten Agenten

- Pfade enthalten Umlaute. Render-Commands verwenden deshalb `cd Smarte*/...`.
- Git-Status kann auf macOS wegen Unicode-Normalisierung komisch aussehen.
- Nicht blind `git reset --hard` verwenden.
- Alte Solar-Felder in DB/Seed sind kein aktuelles Feature mehr.
- Der wichtigste verbleibende Check ist ein End-to-End-Test mit laufender API und
  echter Route, um das neue Depot-Routing live auf der Karte zu beobachten.
